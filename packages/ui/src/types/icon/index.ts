export interface StackIconProps {
  name: string;
  icon: string;
  color?: string;
  size?: number;
  className?: string;
}
export interface StackTagProps {
  name: string;
  icon: string;
  color?: string;
  size?: number;
  className?: string;
  showName?: boolean;
}
